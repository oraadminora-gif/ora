# ── Python / Django ───────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg
*.egg-info/
dist/
build/
.eggs/

# Base de données
*.sqlite3
*.sqlite3.backup

# Fichiers statiques collectés (prod)
staticfiles/
static_root/
media/

# Logs
*.log
logs/

# ── Environnement virtuel ────────────────────────────
.venv/
venv/
env/
ENV/

# ── Variables d'environnement (secrets) ─────────────
# Backend Django — fichier unique avec tous les secrets
.env
.env.local

# Frontend Vite — .env.development et .env.production sont commités (pas de secrets)
# Seuls les overrides locaux sont ignorés
.env.*.local

# ── Sauvegardes base de données ──────────────────────
backups/
backup_*.json
backup_*.sql
deploy/.env.production

# ── Frontend React / Vite ────────────────────────────
node_modules/
dist/
build/
.vite/
*.local

# ── Éditeurs / IDE ───────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── Système d'exploitation ───────────────────────────
.DS_Store
Thumbs.db
desktop.ini
