# Détecte les échecs de connexion JWT (API) : réponse 401 sur /api/auth/login/
# Fichier surveillé : /var/log/nginx/access.log (format combined standard)
[Definition]
failregex = ^<HOST> -.*"POST /api/auth/login/ HTTP/\d\.\d" 401
ignoreregex =
